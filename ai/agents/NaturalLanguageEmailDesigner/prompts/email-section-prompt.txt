You will be provided with HTML content:

{{ reference_html }}

Your task is to improve the HTML to achieve the following goal:

Make it effortless, obvious, and rewarding for the recipient to take action.

Instructions:

Only return the improved HTML.

Do not return a full HTML document (no <html>, <head>, <body> tags unless they were already in the input).

Maintain the overall structure unless changes are needed for clarity or actionability.

Focus on improving readability, visual hierarchy, calls to action (CTAs), and any elements that make the user's next step easier.

Do not explain your changes or include any commentary — output only the improved HTML.